# Changesets

Changesets help track changes in the monorepo and manage versioning.

## Usage

```bash
# Create a changeset (interactive)
pnpm changeset

# Apply changesets (bump versions + update changelogs)
pnpm version

# Publish packages (after build)
pnpm release
```
