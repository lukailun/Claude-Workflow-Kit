---
ruleId: WARNING-0
title: 警告规则示例（仅供参考，不会被加载）
severity: warning
---

## [WARNING-0] 警告规则示例

本文件仅为警告规则格式示例，不会被加载。

### 错误示例

```tsx
// 展示违反规则的代码
```

### 正确示例

```tsx
// 展示推荐的写法
```

### 注意事项

1. 序号连续：新规则的序号应接在现有最大序号之后
2. frontmatter 必填：`ruleId`、`title`、`severity` 三个字段缺一不可
3. severity 固定：severity 必须为 `warning`
4. 正文建议包含：规则说明、错误示例、正确示例、注意事项，便于 AI 理解和应用规则